{
  "name": "rest-express",
  "version": "1.0.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "tsx server/index.ts",
    "build": "cd client && npm install && npm run build && cd ../server && npm install && npm run build",
    "start": "NODE_ENV=production node dist/server/index.js",
    "check": "tsc",
    "db:push": "drizzle-kit push"
  },
  "dependencies": {
    "express": "^4.21.2",
    "nanoid": "^3.3.4",
    "ws": "^8.18.0",
    "passport": "^0.7.0",
    "passport-local": "^1.0.0",
    "memorystore": "^1.6.7",
    // autres dépendances serveur importantes ici
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "esbuild": "^0.25.0",
    "typescript": "5.6.3",
    // autres outils de build & types
  }
}
